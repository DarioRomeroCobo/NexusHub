<nav class="navbar navbar-expand-md bg-white sticky-top shadow-sm py-1">

    <div class="container-fluid">

        <!-- Logo arriba/izquierda -->
        <% if (!isLoggedIn) { %>
            <a class="navbar-brand d-flex align-items-center" href="/" aria-label="Inicio">
                <img src="/img/logo.png" alt="Logo NexusHub" width="72" height="29" class="img-fluid rounded">
            </a>
            <% } %>
                <!-- Botón menú en móvil -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarBar"
                    aria-controls="navbarBar" aria-expanded="false" aria-label="Abrir navegación">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Botones registro e inicio de sesión -->
                <div class="collapse navbar-collapse justify-content-end mb-2" id="navbarBar">
                    <a href="/usuario/ver-usuarios"
                        class="btn btn-outline-primary px-4 py-2 rounded-pill fw-semibold me-2"
                        aria-label="Ver usuarios registrados">
                        Ver usuarios registrados
                    </a>

                    <% if (!isLoggedIn) { %>
                        <a href="/usuario/inicio-sesion"
                            class="btn btn-gradient px-4 py-2 rounded-pill fw-semibold me-2"
                            aria-label="Inicio de sesión" title="Ir a la página de inicio de sesión">
                            Inicio de sesión
                        </a>

                        <a href="/usuario/registro" class="btn btn-gradient px-4 py-2 rounded-pill fw-semibold"
                            aria-label="Registro" title="Ir a la página de registro">
                            Registro
                        </a>
                        <% } else { %>


                            <a href="/usuario/subir-video"
                                class="btn btn-gradient px-4 py-2 rounded-pill fw-semibold me-2"
                                aria-label="Subir video" title="Ir a la página de subir video">
                                Subir video
                            </a>
                            <a href="/usuario/vincular-youtube" class="btn btn-gradient px-4 py-2 rounded-pill fw-semibold me-2"
                        aria-label="Vincular Youtube" title="Ir a la página de vinculación con Youtube">
                        Vincular Youtube
                    </a>
                    
                        <a href="/usuario/logout" class="btn btn-sm rounded-pill btn-outline-danger">Cerrar sesión</a>
                        <% } %>
                </div>
        </div>
</nav>